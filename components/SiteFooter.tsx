/** Footer, shown only on the landing view (hidden once a prompt is selected). */
export function SiteFooter() {
  return (
    <footer>
      <span>Built by Josh Benzadon</span>
      <span>For drafting and analysis support. Not a substitute for attorney review.</span>
    </footer>
  );
}
